document.addEventListener("DOMContentLoaded", event => {
    // checks if word is palindrome
    let isPalindrome =false
    // will display palindrome result in DOM
    let resultContainer =document.getElementById("result")

    // TODO: handle form data to get input word

    // TODO: create if-statement to check if word is a palindrome

    // TODO: update resultContainer to indicate to the user if the word is a palindrome
})